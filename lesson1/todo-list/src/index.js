import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

const rootElement = document.querySelector('#root');

const elememt = (
  <h1 class="title">Todo List</h1>
);

ReactDOM.render(elememt, rootElement);
